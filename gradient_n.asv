function V = gradient_n(prec,U,X,M)

N = length(U);
s = size(X);

r = size(U{1},2);

if prec == 0
    U = cellfun(@(x)half(x),U,'UniformOutput',0);
    ind = [];
    for i = 1:N
        tmp = randi(s(i),M,1);
        ind = [ind,tmp];
    end
    mat = [];
    for i = 1:r
        
    end
end